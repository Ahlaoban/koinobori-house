# Revue complète de la PR #14 — 2026-09-16

Cible : `codex/kh2027-reprise` (`45c2adc`) vers `main` (`23fc05b`), 113 fichiers, +15 744 lignes, dont 49 fichiers de code (thème enfant, plugin `kh-product-media`, `tools/recovery`, `tools/preview`). Méthode : 8 angles indépendants (ligne à ligne, comportements supprimés, traçage inter-fichiers, réutilisation, simplification, efficacité, altitude, conventions CLAUDE.md), 47 candidats, puis vérification individuelle contre le code. Verdicts : CONFIRMÉ = ligne et scénario tenus ; PLAUSIBLE = dépend d'un état réaliste ou d'un comportement de plugin non présent dans l'arbre ; RÉFUTÉ = écarté.

Aucun merge, aucun push, aucun changement WordPress. **Secrets : aucun** dans les 49 fichiers (F5 réfuté ; seuls des identifiants d'infrastructure : nom de base du clone, chemins `/home3/sc3heal3867`, expéditeurs `contact@koinoborihouse.com` et un ancien `kaeljin@koi-nobori.com` à retirer).

## 1. Bloquants avant merge (à corriger dans PR #14 ou immédiatement après, avant tout déploiement staging)

| # | Fichier:ligne | Défaut | Verdict | Correction proposée |
|---|---|---|---|---|
| B1 | `inc/class-icon-walker.php:24` + `inc/header.php:55-57` + `assets/js/header.js:45-47` | Le walker n'émet rien pour un item sans classe `icon-*` ; `fallback_cb=false` ; header.js déréférence `.kh-nav-list` sans garde. **Assigner un vrai menu à `kh_header` (A10) vide la navigation et casse le JS mobile.** | CONFIRMÉ (C1, G2, H1) | Déduire l'icône de l'objet lié (`object_id` → page slug / shop) avec repli sur une icône neutre ; ne jamais retourner sans `<li>` ; garde `if (!nav) return;` dans `layout()` ; test : assigner un menu sans classes et vérifier 5 liens + toggle mobile |
| B2 | `tools/recovery/import_enquiry_notification_drafts.php:165-176` | `Form::getFormsDefaultSettings()` = défauts Fluent Forms, pas la ligne stockée ; `store(meta_id)` écrase `formSettings` (restrictions, layout, redirectTo). Contrôle post-écriture ne compare que `confirmation.messageToShow`. | CONFIRMÉ (H3) | Lire la ligne existante, ne modifier que `confirmation.messageToShow`, comparer la ligne complète après écriture. **Ne pas rejouer sur staging avant correction.** Sauvegarde l.139-146 permet une restauration manuelle sur le clone |
| B3 | `inc/editorial.php:29-38` + `inc/footer.php:33` + `template-parts/footer-horizon.php:24` | Liens légaux rendus seulement si `get_page_by_path(slug FR)` trouve une page **publiée** ; footer Kadence supprimé, aucun repli. | CONFIRMÉ (G3) | Emplacement de menu `kh_footer_legal` (Polylang le scinde par langue) ou repli sur l'URL brute si la page existe non publiée + avertissement admin ; interdire la mise en brouillon des 4 pages légales en prod |
| B4 | `wp/mu-plugins/koino-diag-frontpage.php` + `tools/recovery/set_clone_frontpage.php:40` | `polylang.redirect_lang=true` appliqué au clone seulement ; staging reste `false` (`/fr/` = index du blog) ; mu-plugin diag « TEMPORAIRE, SUPPRIMER » dans l'arbre déployable. | CONFIRMÉ (D7) | Supprimer `koino-diag-frontpage.php` de la PR ; consigner `redirect_lang=true` comme réglage requis (runbook KH-104b/KH-107 + assertion dans `KH-104b-full-tests.ps1`) ; appliquer sur staging en PC4 |

## 2. À corriger avant déploiement staging (PC5)

| # | Fichier:ligne | Défaut | Verdict | Correction |
|---|---|---|---|---|
| S1 | `inc/enquiries.php:46-48` | Honeypot forcé côté serveur ; une page servie par LiteSpeed rendue avant le déploiement n'a pas le champ → rejet de toutes les soumissions jusqu'à purge (dépend du comportement FF 6.x `HoneyPot::verify`, non dans l'arbre). | PLAUSIBLE (G4) | Activer le honeypot **globalement** dans Fluent Forms (réglage exporté), retirer le forçage thème, purger LiteSpeed au déploiement |
| S2 | `inc/enquiries.php:31-40` | Langue du formulaire retrouvée par regex `[fluentform id=N]` sur `post_content` de six slugs (premier match seulement) ; bloc Gutenberg, duplication ou renommage → message anti-spam, validation date, calendrier FR silencieusement perdus. | CONFIRMÉ (D2, D1) | Une seule table `form_id → langue` (ou `pll_get_post_language` de la page hôte) ; messages et validation dans la config du formulaire exportée en JSON |
| S3 | `inc/header.php:71-75` | Fragments panier reconstruits via `pll_current_language()` sur wc-ajax sans cookie `pll_language` → « Panier » sur `/en/`. | PLAUSIBLE (H2) | Lire la langue depuis le référent ou le cookie `koino_lang_pref` dans le fragment, ou rendre le libellé côté JS |
| S4 | `inc/header.php:85`, `inc/footer.php:67-69` | `hide_if_no_translation=1` : sur contenu non traduit (10/20 fiches), le sélecteur n'offre plus l'autre langue. | CONFIRMÉ (G1) | `hide_if_no_translation=0` (Polylang renvoie l'accueil de l'autre langue) |
| S5 | `assets/css/enquiries.css:3,4,92-95`, `kh-editorial.css:3,42` | Cinq couleurs hors palette v2.0 (`#a69d91`, `#655c52`, `#a72715`, `#625b54`, `#eeece7`). | CONFIRMÉ (E1, E2) | Jetons existants `--kh-sumi-20`, `--kh-washi-92` ; hover = sumi comme `.kh-button:hover` ; ou arbitrage Alain pour des teintes dérivées |
| S6 | `kh-product-media.php:101` | Tiret cadratin dans l'aria-label vidéo de la fiche produit. | CONFIRMÉ (E4) | `', '` |
| S7 | `inc/footer.php:43` | Groupe « La maison / The house » au lieu de « Navigation » ; « The house » entre en collision avec le libellé EN de L'Atelier « The House ». | CONFIRMÉ (E5) | Renommer « Navigation » ou arbitrage Alain |
| S8 | `inc/header.php:18` | Seul `kadence_header` est décroché : **aucun formulaire de recherche** sur le site (l'amendement 13/09 ne mentionne pas la recherche ; v2.0 §3 listait « Compte / recherche / panier »). | CONFIRMÉ (G5) | Arbitrage Alain : recherche supprimée volontairement, ou icône recherche à ajouter |
| S9 | `page-templates/kh-home.php:21,27` | `get_header()` sans retirer les hooks titre Kadence → double H1 possible selon Customizer ; `the_content()` jamais rendu. | CONFIRMÉ (G6) | `remove_action` des hooks hero/title dans le template ; voir A1 ci-dessous |
| S10 | `page-templates/kh-home.php:72` | `WP_Query` sans `ignore_sticky_posts` → articles épinglés en plus des 3. | CONFIRMÉ (J2) | `'ignore_sticky_posts' => true` |
| S11 | `tools/recovery/prepare_enquiry_fields.php:15` | `realpath('')` = cwd → `apply` sans BACKUP_DIR passe la garde. | CONFIRMÉ (J3) | Refuser un argument vide |
| S12 | `tools/recovery/stage_snapshot.py:92,107,108` | `read_text()` sans `encoding='utf-8'` alors que l'écriture est UTF-8 `ensure_ascii=False`. | PLAUSIBLE (H5) | `encoding='utf-8'` |
| S13 | `tools/recovery/enquiry_notification_test_runtime.php:99-105` | Comparaison stricte draft/stocké alors que `SettingsService::store` peut normaliser. | PLAUSIBLE (H4) | Comparaison tolérante ou re-lecture post-import |
| S14 | `inc/enquiries.php:61` | Validation date gatée sur `date_souhaitee`/`preferred_date`, noms non consignés dans le dépôt. | PLAUSIBLE (H6) | Exporter les définitions de formulaires en JSON dans Git |

Réfuté : J1 (formats de date non PHP) — `check_enquiry_validation.php` a validé le 29/02/2028 avec le format stocké, 124 contrôles OK sur le clone. A6 (« Arts de vivre absent ») — conforme à l'arbitrage du 16/09.

## 3. Dette et arbitrages produit (non bloquants, à planifier)

| # | Sujet | Verdict | Position |
|---|---|---|---|
| A1 | `kh-home.php` : tout le texte FR/EN en dur dans PHP via `$t()`, `the_content()` jamais appelé ; CLAUDE.md fixe Gutenberg + Kadence Blocks et la homepage est arbitrage Alain. | CONFIRMÉ (D6) | **Arbitrage Alain** : accepter un gabarit codé au lancement (rapide) et migrer en blocs/patterns après le 30/09, ou exiger l'éditable maintenant |
| A2 | `inc/footer.php:42-64` : seconde table de slugs en dur, `get_page_by_path` non sensible à la langue. | CONFIRMÉ (C7, altitude) | Emplacements de menu `kh_footer_services` / `kh_footer_legal` ; même mécanisme que le header |
| A3 | `web_review_guard.php:119-125, 325-331, 364-370` : état du clone fabriqué par requête (6 plugins filtrés, template 318/319 simulé, theme_mod forcé, plugin inactif chargé) → ce qui est revu ≠ ce que déploie la branche. | CONFIRMÉ (F4) | Rendre l'état réel en base (WP-CLI) ; réduire le garde à la sécurité |
| A4 | `header.css:12` : header scrollé `backdrop-filter: blur(14px)` — « cartes vitrées » interdites v2.0 §15 ; la charte prévoit un header washi .92 sans flou. | PLAUSIBLE (E3) | ✅ **Clos le 2026-09-17** : le flou après défilement a été demandé explicitement par Alain dans son prompt de header (relevé par Codex). Validation consignée, pas de nouvelle décision |
| A5 | Perf : `koinobori_child_editorial_page_url()` ~17 appels non mémoïsés par page (D4) ; menu/actions rendus 2× et `pll_the_languages` 3× (C3) ; 6 `get_terms` + 2 WP_Query sur la home (D5) ; `layout()` force un reflow par frame de scroll + blur (C4) ; `kh-charte-v3.css` 30 Ko chargé partout, majoritairement mort (E7) ; preload de l'italique non utilisée au-dessus de la ligne de flottaison (E8). | CONFIRMÉ | Cache statique par requête, rendu unique, scroll handler à seuil, charte réduite aux jetons, preload corrigé |
| A6 | Duplication : helper FR/EN en trois exemplaires (D3) ; sélecteur de langue rendu deux fois avec dérive a11y (C6) ; six slugs en trois listes (D1) ; gardes sandbox copiées dans 9 scripts, bloc secret dans 5, map form-id dans 4 (F1-F3) ; trois tests « suis-je sur le clone » différents (F2) ; listes MIME PHP/JS (E9) ; clé icône listée deux fois dans le walker (C2) ; `khHeaderClaimFirstVisit` global pour un harnais qui ne teste rien (C5). | CONFIRMÉ | `koinobori_child_lang()` unique ; `tools/recovery/lib/guard.php` ; scripts one-shot datés à archiver dans `docs/audit/` |

## 4. Recommandation

- **Ne pas merger PR #14 en l'état.** Quatre bloquants (B1-B4) sont de petite taille (≈ 60 lignes) ; je propose de les corriger sur une branche `fix/pr14-blockers` depuis `45c2adc`, revue Codex, puis merge de #14 mise à jour.
- S1-S14 : lot « corrections avant staging » après merge, avant PC5.
- A1, A4, S7, S8 : quatre arbitrages Alain.
- A2, A3, A5, A6 : dette planifiée après le 30/09, sauf A5 si la recette mobile montre un problème.
