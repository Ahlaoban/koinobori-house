# Lot S — corrections avant déploiement staging (2026-09-21)

Suite de [REVUE-PR14-2026-09-16.md](REVUE-PR14-2026-09-16.md) §2. Branche `fix/pr14-s-lot` depuis `main` `d3f7a57`. Code local uniquement : aucun changement WordPress, aucun accès serveur.

## Corrigé dans le code

| # | Correction | Fichier |
|---|---|---|
| S1 | Honeypot plus forcé par le thème (une page en cache sans le champ rejetait toute soumission). Le message anti-spam FR/EN reste filtré | `inc/enquiries.php` |
| S2 | Langue du formulaire : tous les formulaires d'une page (shortcode quel que soit l'ordre des attributs, ou bloc Fluent Forms), langue = langue Polylang de la page hôte, table de slugs en repli | `inc/enquiries.php` |
| S3 | Libellés du panier en `wc-ajax` : la langue vient du référent (`/fr/` ou `/en/`) | `inc/header.php` |
| S4 | `hide_if_no_translation = 0` dans le header et le footer : l'autre langue reste toujours proposée | `inc/header.php`, `inc/footer.php` |
| S5 | Cinq couleurs hors palette remplacées par des jetons : traits `--kh-sumi-52`, texte atténué `--kh-sumi-70`, survol du bouton = sumi (comme `.kh-button:hover`), erreurs = vermillon, fond de section = `--kh-white` | `enquiries.css`, `kh-editorial.css` |
| S6 | Tiret cadratin retiré de l'`aria-label` vidéo (virgule) ; plugin 1.0.1 | `kh-product-media.php` |
| S7 | Groupe du footer « Navigation » (arbitrage Alain du 21/09) | `inc/footer.php` |
| S9 | **Requalifié après vérification à la source** (Kadence 1.5.2) : le titre de page n'est émis que par `kadence_single`, que ce gabarit n'appelle pas, donc pas de double H1. Défaut réel trouvé : `<main>` imbriqué dans le `<main id="inner-wrap">` de Kadence, remplacé par `<div id="main">` (comme `template-parts/content/single.php` de Kadence) | `kh-home.php` |
| S10 | `ignore_sticky_posts` sur les 3 articles de la home | `kh-home.php` |
| S11 | `prepare_enquiry_fields.php` refuse un dossier de sauvegarde vide (`realpath('')` valait le répertoire courant) | `tools/recovery/` |
| S12 | `read_text(encoding='utf-8')` ; 14 tests `test_stage_snapshot.py` verts | `tools/recovery/stage_snapshot.py` |
| S13 | Comparaison brouillon / stocké tolérante aux clés ajoutées par la normalisation | `tools/recovery/enquiry_notification_test_runtime.php` |

Thème enfant **1.2.0** : le staging porte encore un 1.1.0 antérieur à la PR #14, le numéro permet de constater le déploiement.

S8 (recherche) : clos par arbitrage du 21/09, pas de recherche au lancement.

## Non vérifié

Aucun interpréteur PHP sur le poste : les fichiers PHP modifiés n'ont **pas** été passés à `php -l`. À faire sur le serveur avant la bascule du thème (étape déjà prévue du PC5).

## À faire sur le staging au PC5 (écritures, session cPanel d'Alain)

1. **Honeypot global** : Fluent Forms → Réglages globaux → activer le honeypot. Conséquence de S1 : sans ce réglage, plus aucun honeypot.
2. **Purge LiteSpeed** après la bascule du thème.
3. **`<title>` de `/en/`** : il affiche « Koinobori originaux signés BCDG ». Ce n'est pas du code de thème : traduire le slogan du site dans Polylang → Traductions des chaînes, et vérifier le titre d'accueil EN dans SEOPress. Dépend aussi de `blogname` (vide, correction PC4 I5).
4. **S14** : exporter les 8 définitions de formulaires Fluent Forms en JSON et les ranger dans Git (`docs/lot3/forms/`), pour que les noms de champs (`date_souhaitee`, `preferred_date`, `pays`, `country`) sur lesquels le thème s'appuie soient consignés.
5. Recette : soumission de chaque formulaire en FR et en EN, ajout au panier depuis `/en/` (libellé « Cart »), sélecteur de langue sur une fiche non traduite.
